import type { ReactNode } from "react";

/** Placeholder. M2 fills this in with route list and encounter logging. */
export function RoutesScreen(): ReactNode {
  return <h1 className="p-4 text-xl">Routes</h1>;
}
