import type { ReactNode } from "react";

/** Placeholder. M1 fills this in with the actual run list. */
export function RunListScreen(): ReactNode {
  return <h1 className="p-4 text-xl">Run List</h1>;
}
